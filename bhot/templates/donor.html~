{% extends "layout.html" %}
{% from "_form_helpers.html" import render_bootstrap_form_field %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-xs-6">
            <h3>Recipient</h3>

            <form action="" method="post" class="form" role="form">
                {{ form.csrf_token() }}

                {{ render_bootstrap_form_field(form.name, class='form-control', placeholder='Your Name') }}
                {{ render_bootstrap_form_field(form.gender, class='form-control') }}
                {{ render_bootstrap_form_field(form.date_of_birth, class='form-control') }}

                <input type=submit class='btn btn-primary btn-block'>
            </form>
        </div>
    </div>
</div>
{% endblock %}
